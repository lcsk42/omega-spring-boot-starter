server:
  port: 18080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    git:
      mode: full
  metrics:
    tags:
      application: ${spring.application.name}
