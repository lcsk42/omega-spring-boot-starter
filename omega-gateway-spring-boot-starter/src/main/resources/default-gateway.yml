server:
  port: 18080

spring:
  cache:
    type: caffeine
    spec: maximumSize=500, expireAfterWrite=30s

management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    git:
      mode: full
  metrics:
    tags:
      application: ${spring.application.name}
