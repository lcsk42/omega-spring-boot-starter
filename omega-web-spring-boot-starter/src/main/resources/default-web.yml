# 使用随机端口
server:
  port: 0

# 开启
management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    git:
      mode: full
  metrics:
    tags:
      application: ${spring.application.name}